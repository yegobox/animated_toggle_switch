name: animated_toggle_switch
description: Simple and animated switch for multiple choices. It's a good alternative if you don't want to use something like a dropdown menu.
version: 0.2.1
repository: https://github.com/SplashByte/animated_toggle_switch
publish_to: "none"
environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
# dev_dependencies:
#   # The "flutter_lints" package below contains a set of recommended lints to
#   # encourage good coding practices. The lint set provided by the package is
#   # activated in the `analysis_options.yaml` file located at the root of your
#   # package. See that file for information about deactivating specific lint
#   # rules and activating additional ones.
#   flutter_lints: ^2.0.0
